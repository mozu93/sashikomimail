# リリースノート

## v1.1.0 — 2026-07-28

### 追加

- 起動時にGitHub Releasesから最新バージョンを確認
- 新しいバージョンがある場合に画面上部へ更新通知を表示
- 更新用インストーラーの進捗付きダウンロード
- 「今すぐ更新」によるアプリ終了・サイレント更新・再起動
- mainへのpush／Pull Requestで15件のテストを実行するGitHub Actions
- `v*`タグからWindowsインストーラーとGitHub Releaseを作成するGitHub Actions

### 安全性

- 更新確認に失敗しても通常機能は継続
- 更新ファイルの取得元をGitHubのHTTPS URLに制限
- 開発版から誤って自動インストールを実行しない保護を追加

## v1.0.0 — 2026-07-28

差し込みメール送信アプリの初回正式リリースです。

### 主な機能

- Excel（`.xlsx` / `.xls`）・CSVから宛先データを読み込み
- 任意列をTo・CCとして選択し、固定BCCを設定
- Excel・CSVのヘッダーから差し込みタグを自動生成
- 件名・本文・署名への差し込み
- 空欄時に敬称などを非表示にする条件付きタグ
- 保存名簿、テンプレート、署名の登録・編集・削除
- 任意列に対するデータフィルタリング
- 選択行の送信プレビュー
- 共通添付と事業所別の個別添付
- 添付点数・容量・残容量の表示
- Microsoft Graph APIとOAuth2によるMicrosoft 365送信
- テスト送信、一括送信、進捗表示、送信中断
- テスト・一括・再送を区別した送信履歴
- エラー・未送信対象のみの再送信
- 429応答時の待機と再試行
- 5件連続失敗時の安全中断

### 安全性とデータ保護

- 送信中の二重起動と編集操作を禁止
- 一括送信前に認証アカウント・差出人・送信件数を表示
- 送信前に宛先形式、重複宛先、未定義タグ、添付容量を検査
- 保存名簿、送信対象、宛先、件名、エラー情報をWindows DPAPIで暗号化
- OAuthトークンをOS暗号化付きキャッシュへ保存
- 送信履歴の保存期間設定と個別削除
- データベースの手動バックアップ
- 未処理例外をローカルログへ記録

### 配布

- バージョン表示：`1.0.0`
- PyInstallerによるWindows EXEビルドに対応
- Inno Setup用インストーラー定義を同梱
- デスクトップ・タスクバー用アイコンを同梱

### 動作環境

- Windows 10 / 11（64ビット）
- Microsoft 365アカウント
- Microsoft Entra IDアプリ登録
- 委任されたMicrosoft Graph `Mail.Send`アクセス許可
- 代理差出人を利用する場合は`Mail.Send.Shared`とメールボックス側の送信権限

### 既知の制約

- メール本文はプレーンテキスト形式です。
- 共通添付と個別添付の合計は、1通あたり安全上限2.5MBです。
- 3MBを超える大容量添付のアップロードセッションには未対応です。
- 保存データのDPAPI暗号は、原則として保存したWindowsユーザーとPCで復号します。
- Microsoft 365は大量の広告・ニュースレター配信専用サービスではありません。
- 同時に複数PCから1つのデータベースを共有する運用には対応していません。

### 検証結果

- 自動テスト：13件合格
- Python依存関係検査：問題なし
- PyInstallerビルド：成功
- 配布用EXE起動確認：成功
