# リリースノート

## v1.1.6 — 2026-08-06

### 修正

- 宛先の重複エラーに表示される行番号が、Excel / CSVの実際の行番号ではなく
  絞り込み後の並び順の番号になっていた問題を修正
- 上記の影響で、絞り込み条件を変えるたびにエラー内容の文言が変わり、
  一度「選択行のエラーを確認・有効化」で有効化した行の確認済み状態が
  解除され、送信が停止することがあった問題を解消
- 送信中に「一括送信」「送信を中止」ボタンの配色が変わらず、押せない状態か
  判別できなかった問題を修正。操作できない間はグレーで表示する

## v1.1.5 — 2026-08-03

### 修正

- OSがダークモードのPCで、プルダウン・入力欄・リストの文字が
  白背景に白文字となり読めなくなる問題を修正。背景色を指定した箇所へ
  文字色を明示し、ドロップダウンの一覧・タブ・進捗バー・メニュー・
  ツールチップの配色も指定
- スクロール領域の背景が暗いままとなり、「3. 宛先設定」「4. 件名・本文」
  などの見出しが読めなくなる問題を修正。スクロールバーとスプリッターの
  配色も統一
- 起動時に明るい配色を明示的に適用し、OS側のテーマ設定に影響されない
  ようにした

### 改善

- 作成・送信タブのプルダウン（To列、CC列、差出人、テンプレート、署名）が
  マウスホイールで切り替わらないよう変更。誤って選択が変わることを防ぐ。
  ホイール操作は画面のスクロールとして動作する
- 本文入力欄の高さを1.5倍に拡大

## v1.1.4 — 2026-08-03

### 追加

- 送信中の進捗表示に、その時点で送信している宛先アドレスと
  添付ファイル名を表示
- 送信前に、打ち間違いが疑われるドメイン（`dokomo.ne.jp`など）を検出して
  想定される正しいドメインとともに警告
- 送信前に、携帯キャリア宛の件数をドメインごとに集計して警告し、
  指定受信設定の案内を促す
- 設定画面の代理差出人欄に、配信不能通知と返信が代理差出人アドレスへ
  届くことを明示

### 改善

- 送信結果・送信履歴の「成功」を「受理」に変更し、Microsoft 365が
  送信要求を受け付けた状態であって到達保証ではないことを明記

## v1.1.3 — 2026-07-30

### 追加

- 名簿の重複宛先などでエラーになった行について、エラー内容と行データを
  確認して個別に有効化できる機能を追加
- 確認済みの行をデータプレビュー上で判別できる「確認済み」表示を追加

### 安全性

- 未確認のエラー行が残っている場合は、行番号とエラー内容を表示して送信を停止
- 行データの編集、To列・CC列の変更、行削除後は確認済み状態を自動的に解除
- 確認したエラー内容が変化した場合は、以前の確認を無効化して再確認を要求

## v1.1.2 — 2026-07-30

### 改善

- 個別添付の照合キーを、Excel・CSVのヘッダーから選択する2項目へ拡張
- 選択した2列の値を`1項目目_2項目目`の形式でつなぎ、個別添付の
  ファイル名と照合
- 個別添付の設定直後に、行ごとの照合値と割り当てられたファイル名を表示
- 個別添付が割り当てられていない送信対象がある場合、行番号と照合値を
  送信前に警告
- 一括送信の最終確認画面に、個別添付の割当件数と照合列を表示
- 照合対象列の値を編集した場合、誤送信防止のため既存の割当を自動解除

## v1.1.1 — 2026-07-28

### 追加

- メール作成画面で、送信ごとに「自分のアドレス」または設定済みの
  「代理差出人」を選択できる差出人欄を追加
- 選択した差出人をテスト送信、一括送信、送信履歴からの再送へ反映
- 一括送信の最終確認画面に、実際に使用する差出人を表示

### 改善

- 自分のアドレスを選択した場合は通常の`Mail.Send`、代理差出人を選択した場合は
  `Mail.Send.Shared`を使用するよう、選択内容に応じて認証権限を切り替え
- 設定画面で代理差出人を変更した際、メール作成画面の選択肢を自動更新

## v1.1.0 — 2026-07-28

### 追加

- 起動時にGitHub Releasesから最新バージョンを確認
- 新しいバージョンがある場合に画面上部へ更新通知を表示
- 更新用インストーラーの進捗付きダウンロード
- 「今すぐ更新」によるアプリ終了・サイレント更新・再起動
- mainへのpush／Pull Requestで15件のテストを実行するGitHub Actions
- `v*`タグからWindowsインストーラーとGitHub Releaseを作成するGitHub Actions

### 安全性

- 更新確認に失敗しても通常機能は継続
- 更新ファイルの取得元をGitHubのHTTPS URLに制限
- 開発版から誤って自動インストールを実行しない保護を追加

## v1.0.0 — 2026-07-28

差し込みメール送信アプリの初回正式リリースです。

### 主な機能

- Excel（`.xlsx` / `.xls`）・CSVから宛先データを読み込み
- 任意列をTo・CCとして選択し、固定BCCを設定
- Excel・CSVのヘッダーから差し込みタグを自動生成
- 件名・本文・署名への差し込み
- 空欄時に敬称などを非表示にする条件付きタグ
- 保存名簿、テンプレート、署名の登録・編集・削除
- 任意列に対するデータフィルタリング
- 選択行の送信プレビュー
- 共通添付と事業所別の個別添付
- 添付点数・容量・残容量の表示
- Microsoft Graph APIとOAuth2によるMicrosoft 365送信
- テスト送信、一括送信、進捗表示、送信中断
- テスト・一括・再送を区別した送信履歴
- エラー・未送信対象のみの再送信
- 429応答時の待機と再試行
- 5件連続失敗時の安全中断

### 安全性とデータ保護

- 送信中の二重起動と編集操作を禁止
- 一括送信前に認証アカウント・差出人・送信件数を表示
- 送信前に宛先形式、重複宛先、未定義タグ、添付容量を検査
- 保存名簿、送信対象、宛先、件名、エラー情報をWindows DPAPIで暗号化
- OAuthトークンをOS暗号化付きキャッシュへ保存
- 送信履歴の保存期間設定と個別削除
- データベースの手動バックアップ
- 未処理例外をローカルログへ記録

### 配布

- バージョン表示：`1.0.0`
- PyInstallerによるWindows EXEビルドに対応
- Inno Setup用インストーラー定義を同梱
- デスクトップ・タスクバー用アイコンを同梱

### 動作環境

- Windows 10 / 11（64ビット）
- Microsoft 365アカウント
- Microsoft Entra IDアプリ登録
- 委任されたMicrosoft Graph `Mail.Send`アクセス許可
- 代理差出人を利用する場合は`Mail.Send.Shared`とメールボックス側の送信権限

### 既知の制約

- メール本文はプレーンテキスト形式です。
- 共通添付と個別添付の合計は、1通あたり安全上限2.5MBです。
- 3MBを超える大容量添付のアップロードセッションには未対応です。
- 保存データのDPAPI暗号は、原則として保存したWindowsユーザーとPCで復号します。
- Microsoft 365は大量の広告・ニュースレター配信専用サービスではありません。
- 同時に複数PCから1つのデータベースを共有する運用には対応していません。

### 検証結果

- 自動テスト：13件合格
- Python依存関係検査：問題なし
- PyInstallerビルド：成功
- 配布用EXE起動確認：成功
